# 组织架构数据关联验证报告

## 验证范围
- 组织层级：01 → 1.1 → 02 → 03
- 字段完整性：组织编码、名称、负责人、人数、父级编码
- 业务联动：HC申请部门选择、配额计算、提交流程携带组织路径

## 校验结果
- 根节点存在：通过（`01-PW`）
- 父子关系完整：通过（所有非根节点均可追溯父级）
- 人数合法性：通过（无负数）
- 组织路径生成：通过（可回溯完整路径）
- 权限筛选：通过（超权限节点禁用）
- 组件联动：通过（选择组织后携带 `code/name/path`）

## 运行方式
1. 执行初始化脚本：`scripts/initOrgArchitecture.ts`
2. 执行单元测试：
   - `tests/components/DepartmentTreeSelect.spec.ts`
   - `src/views/__tests__/TransitionHCApply.spec.ts`

## 接口关联
- 部门人数：`/api/dept/headcount?deptId=xxx`
- 历史有效数量：`/api/transition-hc/valid-count?deptId=xxx`
- 组织维护：通过 `createOrgNode/updateOrgNode/deleteOrgNode` 提供后台能力映射
